(function() {

    window.App.Views.AppSettings = function () {

        var initialize = function (view) {
        };

        this.templateId = "app-settings-template";
        this.initialize = initialize;
    };

})();